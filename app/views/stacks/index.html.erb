
<ul>
  <% for stack in @stacks %>
    <li class="stack_and_tag_list" id="<%= stack.id %>"><%= stack.name %> | <%= link_to 'View', stack_path(stack.id) %> | <%= link_to 'Delete Stack', stack, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>
</div>

