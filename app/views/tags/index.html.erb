<ul>
  <% for tag in @tags %>
    <li class="stack_and_tag_list" id="<%= tag.id %>"><%= tag.name %> | <%= link_to 'View', tag_path(tag.id) %> | <%= link_to 'Delete Tag', tag, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  <% end %>
</ul>